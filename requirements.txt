aiogram>=3.4.0
python-dotenv>=1.0.0
asyncpg>=0.29.0
APScheduler>=3.10.4
tzdata>=2024.1

# Optional: enables "share my location" timezone auto-detect in /settings.
# The bot works fine without it (manual timezone selection still works) -
# comment this out if you don't want the extra download.
timezonefinder>=6.4.0
